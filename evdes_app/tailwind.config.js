/** @type {import('tailwindcss').Config} */
export default {
  content: [
    './src/**/*.{html,js,vue}', // Include all .vue, .js, and .html files in src directory
    './public/index.html',       // Include the main index.html if using one in public
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

